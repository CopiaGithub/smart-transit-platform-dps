{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.\\SQLEXPRESS;Database=transit_display_platform_local;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True;"
  },
  "Seed": {
    "EnableDemoData": true,
    "AdminPassword": "DpsSeed@2026!tmp",
    // Development only — rewrites every demo account to password 123456 and
    // gives the five test roles single-digit mobile numbers. Refuses to run in
    // any other environment.
    "SimpleDevLogins": true,
    "DevPassword": "123456",
    // Development only — empties the open session's board on every start,
    // so testing begins from nothing. Set false to keep entries across restarts.
    "ClearBoardOnStart": true
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.AspNetCore": "Warning"
      }
    }
  }
}
